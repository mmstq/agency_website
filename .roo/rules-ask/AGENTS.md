# Ask Mode

- Follow [`../../AGENTS.md`](../../AGENTS.md) first.
- Use `.planning/STATE.md` for the current handoff, `.planning/PROGRESS.md` for status, and `.planning/PROJECT.md` for stable context.
- Use `DESIGN.md` for current visual rules; `DESIGN_LOG.md`, `CURSOR_ANIMATION_DESIGN.md`, and `.planning/research/` are historical rationale.
- Confirm claims against current routes, dependencies, and call sites before answering.
- Label work as implemented, technically checked, visually accepted, or production-validated; do not collapse those states.
- Current launch gaps include visual acceptance, placeholder contact/social destinations, Blog depth, and missing browser regression coverage.
